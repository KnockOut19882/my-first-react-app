import styles from './Favorites.module.scss';

const Favorites = () => (
  <div className={styles['favorites']}>
    <h1>Favorites</h1>
    <p>This is the favorites page of our application.</p>
  </div>
);

export default Favorites;